<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ngekos.Yuk - Hunian Nyaman</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/dist/css/all.min.css">
    
    <style>
        body { font-family: 'Poppins', sans-serif; background-color: #f8f9fa; }
        .hero-section { background: linear-gradient(135deg, #0d6efd 0%, #003d99 100%); color: white; padding: 60px 0; margin-bottom: 20px; border-radius: 0 0 50px 50px; }
        .card { border: none; border-radius: 15px; transition: transform 0.3s ease, box-shadow 0.3s ease; overflow: hidden; }
        .card:hover { transform: translateY(-10px); box-shadow: 0 10px 20px rgba(0,0,0,0.1); }
        .status-badge { position: absolute; top: 15px; right: 15px; border-radius: 20px; padding: 5px 15px; font-size: 0.8rem; font-weight: 600; z-index: 10; }
        .price-tag { color: #0d6efd; font-size: 1.25rem; font-weight: 700; }
        .filter-container { background: white; padding: 15px 25px; border-radius: 30px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); display: inline-flex; align-items: center; gap: 10px; }
        .filter-btn { border-radius: 20px; font-size: 0.9rem; padding: 5px 20px; text-decoration: none; transition: 0.3s; border: 1px solid #dee2e6; color: #6c757d; }
        .filter-btn.active { background-color: #0d6efd; color: white; border-color: #0d6efd; }
    </style>
</head>
<body class="bg-light">

    <nav class="navbar navbar-expand-lg navbar-dark bg-transparent position-absolute w-100">
        <div class="container">
            <a class="navbar-brand fw-bold fs-3" href="/">Ngekos.Yuk</a>
            <a href="/login" class="btn btn-light btn-sm px-4 fw-bold shadow-sm">Login Pemilik</a>
        </div>
    </nav>

    <div class="hero-section shadow">
        <div class="container text-center mt-4">
            <h1 class="display-4 fw-bold">Temukan Kos Impianmu</h1>
            <p class="lead">Proses cepat, informasi akurat, dan harga bersahabat.</p>
        </div>
    </div>

    <div class="container text-center mb-5">
        <div class="filter-container">
            <span class="fw-bold me-2"><i class="fas fa-filter text-primary"></i> Filter:</span>
            <a href="/" class="filter-btn <%= !currentFilter ? 'active' : '' %>">Semua</a>
            <a href="/?status=Tersedia" class="filter-btn <%= currentFilter === 'Tersedia' ? 'active' : '' %>">Tersedia</a>
            <a href="/?status=Terisi" class="filter-btn <%= currentFilter === 'Terisi' ? 'active' : '' %>">Terisi</a>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <% if(kamars.length > 0) { %>
                <% kamars.forEach(kamar => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 shadow-sm">
                            <span class="status-badge shadow-sm <%= kamar.status_kamar === 'Tersedia' ? 'bg-success text-white' : 'bg-danger text-white' %>">
                                <%= kamar.status_kamar.toUpperCase() %>
                            </span>
                            
                            <div class="card-body p-4 mt-2">
                                <h4 class="card-title fw-bold"><%= kamar.jenis_kamar %></h4>
                                <p class="text-muted mb-4" style="min-height: 50px;"><%= kamar.deskripsi %></p>
                                
                                <div class="mb-3">
                                    <small class="text-muted d-block">Harga Sewa</small>
                                    <span class="price-tag">Rp <%= kamar.harga.toLocaleString('id-ID') %></span>
                                    <small class="text-muted">/bln</small>
                                </div>

                                <% if(kamar.status_kamar === 'Tersedia') { %>
                                    <hr>
                                    <form action="/booking/<%= kamar.id_kamar %>" method="POST">
                                        <input type="hidden" name="jenis_kamar" value="<%= kamar.jenis_kamar %>">
                                        <input type="hidden" name="harga_kamar" value="<%= kamar.harga %>">
                                        
                                        <div class="mb-2">
                                            <input type="text" name="nama_penyewa" class="form-control form-control-sm" placeholder="Nama Lengkap" required>
                                        </div>
                                        <div class="mb-3">
                                            <input type="number" name="no_hp" class="form-control form-control-sm" placeholder="Nomor WhatsApp Anda" required>
                                        </div>
                                        
                                        <button type="submit" class="btn btn-success w-100 shadow-sm d-flex align-items-center justify-content-center py-2">
                                            <i class="fab fa-whatsapp me-2 fs-5"></i> Booking & Hubungi WA
                                        </button>
                                    </form>
                                <% } else { %>
                                    <div class="alert alert-secondary text-center mb-0 mt-3 py-2 small fw-bold">
                                        <i class="fas fa-ban me-1"></i> Maaf, Kamar Sudah Terisi
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>

    <footer class="text-center py-5 text-muted">
        <div class="container">
            <hr>
            <p>&copy; 2026 Ngekos.Yuk - Hunian Nyaman Pilihan Mahasiswa</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>